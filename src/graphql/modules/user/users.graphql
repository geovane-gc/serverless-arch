type Mutation {
  createUser(input: CreateUserInput!): User
}

input CreateUserInput {
  email: String!
  password: String!
}

type User {
  userId: ID!
  email: String!
  password: String!
  status: String!
  createdAt: AWSDate
  updatedAt: AWSDate
  deletedAt: AWSDate
}
